export type loginProps = {
    token: string,
    clientId: string,
    userId: string
}
export type mainClassProps = {
    Intents: string[]
    //todo implement in v2.5 chatBot
    //chatBot: boolean
}
